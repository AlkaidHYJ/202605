

# 企业智能数据洞察与数字员工协同平台
## 需求文档 V1.0

部署模式：企业内部私有化部署（单实例/本地集群）

核心定位：面向业务人员与数据运营团队的**采-洗-析-问-协**一体化AI数据中台

---

## 一、三端架构与职责边界

| 端       | 核心职责                                                     | 覆盖模块                                                     |
| -------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| 用户端   | 数据交互、智能问数、独立通讯、数字员工使用、个人工作台       | NL2SQL 问数、独立 IM 聊天（好友 / 群 / 数字员工）、数字大屏查看、消息卡片交互 |
| 管理端   | 组织管控、IM 运营与合规、任务编排、规则设计、AI 配置、系统监控 | 用户与组织管理、群管理、聊天消息管理、爬虫 / 清洗 / 分析 / 大屏 / 员工 / 模型 /Skill 管理 |
| 后台服务 | 数据采集、ETL 清洗、AI 推理、IM 通信与合规、安全执行、运维监控 | 分布式爬虫调度、清洗规则引擎、LLM 网关、NL2SQL 沙箱、IM 合规路由与敏感词过滤、认证鉴权 |

---

## 二、用户端功能需求（业务人员视角）

| 模块               | 功能点                                                       | 交互与体验说明                                               | 验收标准                      |
| ------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ | ----------------------------- |
| 智能问数（NL2SQL） | 自然语言输入框、执行结果表格 / 图表、历史会话、一键导出      | 输入问题 → AI 生成 SQL → 沙箱执行 → 返回结果 + 解读。非 SELECT 提示拦截 | 响应 <5s；支持图表切换        |
| 独立通讯模块（IM） | 好友列表、单聊、群聊、数字员工 @ 答疑、消息类型（文本 / 文件 / 数据卡片） | 群设置可“邀请数字员工加入”；员工监听 @ 触发答疑；发送消息若命中敏感词直接拦截或打标 | 消息延迟 <200ms；支持离线同步 |
| 数字员工交互       | 员工广场、专属对话页                                         | 选择员工 → 多轮对话 → 调用 Skill 与结构化数据                | 上下文保留 20 轮              |
| 数字大屏查看       | 已发布大屏浏览、全屏投屏模式、定时自动刷新                   | 按权限查看数据看板                                           | 首屏加载 <2s                  |
| 个人工作台         | 报表、系统通知快捷入口、最近问数、收藏                       | 聚合常用功能，接收各类提醒                                   | 支持自定义布局                |

---

## 三、管理端功能需求（运营/IT管理员视角）

| 模块                   | 功能点                                                   | 详细说明                                                     | 验收标准                                              |
| ---------------------- | -------------------------------------------------------- | ------------------------------------------------------------ | ----------------------------------------------------- |
| 用户与组织管理         | 组织架构、用户账号、角色与权限（RBAC）、认证配置         | 部门树维护；账号生命周期；角色定义（超管 / 运营 / 只读等）+ 数据权限；LDAP/SSO 对接 | 支持千人级秒级加载；权限精确到行级                    |
| 群管理                 | 群列表与详情、群成员管理、群状态管控、群数据统计         | ① 查看全量群列表（群名 / 群主 / 人数 / 创建时间），支持多条件搜索<br>② 查看 / 导出群成员列表，支持强制移除成员（含数字员工）<br>③ 群状态管控：全员禁言、解散群、关闭群内数字员工自动应答<br>④ 统计：群消息量趋势、活跃度排行 | 支持万级群组检索；操作响应 <1s；解散群后数据软删除    |
| 聊天消息管理           | 会话记录查询、敏感词拦截记录、违规消息处置、消息合规导出 | ① 会话检索：按发送人 / 群聊 / 时间 / 关键词 / 消息类型（文本 / 卡片）穿透查询聊天记录<br>② 敏感记录：查看命中敏感词的消息明细<br>③ 违规处置：对单条消息强制撤回（对端不可见）、对发送人禁言 / 封号<br>④ 合规审计：按条件导出聊天记录存档（支持 PDF/Excel） | 支持亿级消息秒级检索（ES 架构）；强制撤回全端生效 <3s |
| 爬虫与管理             | 爬虫源配置、任务调度、运行监控、日志追踪                 | 支持可视化解析配置，异常重试与告警                           | 调度延迟 <1s                                          |
| 可视化清洗规则配置器   | 规则编排面板、5 类清洗节点、预览、版本管理               | HTML 转纯文本 / 去重 / 标准化 / 脱敏 / 实体抽取              | 实时预览对比                                          |
| 数据资产与分析         | 数据目录、分类占比统计、行业分析 Prompt 配置             | 自动归类；LLM 生成“行业摘要”；审核发布                       | 报告支持导出                                          |
| 数字员工 / 模型 /Skill | 员工 / 模型 / 工具的 CRUD 与编排绑定                     | 员工人设配置、模型路由、Skill 参数配置                       | 测试后方可上线                                        |
| 系统与安全             | 操作审计、配额管理、NL2SQL 安全策略配置                  | 全量操作留痕；SSO 配置查询                                   | 审计日志防篡改                                        |

---

## 四、后台服务需求（技术架构层）

| 服务层            | 核心组件                                                     | 职责与技术实现                                               |
| ----------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| 认证鉴权中心      | 统一身份网关、RBAC 引擎、数据权限拦截器                      | 对接 SSO；分发 JWT；注入数据权限条件                         |
| IM 合规与通信服务 | WebSocket 集群、敏感词过滤引擎、消息审计中心、管理指令下发通道 | ① 敏感词：基于 DFA 算法 + 正则匹配，命中后阻断发送或打标放行<br>② 离线 / 历史消息落盘 ES，支持全文检索<br>③ 接收管理端指令：执行强制撤回、禁言、解散群组广播 |
| 采集与 ETL 引擎   | 分布式调度器、规则解析器、流水线执行器                       | 网页抓取；DAG 清洗任务执行                                   |
| AI 推理与安全沙箱 | LLM 路由、NL2SQL 生成器、AST 解析器、隔离容器                | 模型负载均衡；SQL 仅允许 SELECT；强制超时熔断                |

---

## 五、核心业务流程（关键路径）

### 1. IM消息发送与合规拦截流程
用户端发送消息 → IM网关接收 → 【敏感词引擎】校验 →
- 若命中阻断词：直接丢弃并返回“含违规内容”提示 →
- 若命中审计词：放行下发，但消息打标“已审计”入库 →
- 若正常：下发至接收端 → 消息异步写入ES与关系库备用查询

### 2. 管理端违规消息处置流程
管理员在消息管理中检索命中关键词 → 查看上下文 → 点击“强制撤回” →
后台更新DB消息状态为“已撤回” → 通过WebSocket向客户端广播撤回指令 →
客户端隐藏该消息并显示“该消息因违规被管理员撤回”

---

## 六、安全与合规要求（私有化核心）

| 维度           | 控制策略                                                     |
| -------------- | ------------------------------------------------------------ |
| IM 内容安全    | 强制配置敏感词库（政治 / 暴力 / 泄密等）；支持阻断和审计两级策略；机密数据查询结果卡片禁止转发至外部群组 |
| 越权防护       | IM 中数字员工执行 Skill 前，需校验调用者数据权限；管理员查看聊天记录需具备最高级合规审计权限 |
| 数据隔离与留存 | 满足等保合规要求，所有 IM 记录、操作日志、SQL 执行记录留存 ≥180 天，日志不可篡改 |
| 抗抵赖性       | 消息发送、撤回、管理员干预等操作记录原始操作人与时间戳       |

---

## 七、核心数据表结构建议（草案）

| 表名                                                      | 关键字段                                                     | 用途                               |
| --------------------------------------------------------- | ------------------------------------------------------------ | ---------------------------------- |
| sys_dept / sys_user / sys_role                            | 略                                                           | 组织、用户、角色与数据权限         |
| im_group                                                  | id, group_name, owner_id, avatar, status(1 正常 2 禁言 3 解散), is_bot_enabled | 群组基础信息与状态管控             |
| im_group_member                                           | id, group_id, user_id, role(1 群主 2 管理员 3 普通成员 4 数字员工), join_time | 群成员与数字员工关系映射           |
| im_message                                                | id, chat_type, sender_id, receiver_id/group_id, content, msg_type, is_recalled, audit_status | 消息持久化（同步写入 ES 支持检索） |
| im_sensitive_word                                         | id, word, level(1 阻断 2 审计), category                     | 敏感词库配置                       |
| im_message_audit_log                                      | id, message_id, hit_word, hit_level, operator, action(1 阻断 2 打标 3 撤回) | 合规审计日志                       |
| crawler_task / cleaning_rule / digital_agent / nl2sql_log | 略                                                           | 采洗问数相关核心表                 |

---

## 变更记录
V1.2：新增管理端【群管理】与【聊天消息管理】；新增后台IM合规拦截与强制撤回流程；新增敏感词引擎与ES消息检索架构；补充IM相关核心数据库表设计（im_group、im_message 等）。

## 后端架构
主要的技术栈是：后端框架（Flask、Django、tornado、FastAPI）、sqlalchemy、数据库mysql、鉴权jwt、缓存redis

### 安装所需的库
```bash
pip install "fastapi[all]"
pip install sqlalchemy
pip install pymysql
pip install pyjwt


```